<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /premiertax/public/

    # Force correct asset handling
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    RewriteRule ^ index.php [L]
</IfModule>